// PRODUCTION settings (used by `ng build` / Vercel).
// Replace this with your real Render API URL before deploying.
export const environment = {
  production: true,
  apiUrl: 'https://gmeetsummary-api.onrender.com',
};
